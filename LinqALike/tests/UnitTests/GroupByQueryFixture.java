package UnitTests;

import LinqALike.LinqingList;
import LinqALike.Queryable;
import junit.framework.TestCase;
import org.junit.Test;

import java.util.List;

import static org.fest.assertions.Assertions.assertThat;

/**
 * Created by Edwin on 14/04/03.
 */
public class GroupByQueryFixture extends QueryFixtureBase {

	@Test
	// I'd need to know how groupBy works in order to implement this test.
	public void when_grouping_by_() {
/*		//setup
		LinqingList<Integer> numbers = new LinqingList<>(3, 10, 8, 2, 1, 4, 5, 9, 0);

		//act
		Queryable<Queryable<Integer>> result = numbers.groupBy(number -> number.intValue());

		//assert
		assertThat(numbers.get(0)).isEqualTo(0);*/
	}
}
